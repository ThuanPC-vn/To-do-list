Difficulties encountered during the coding process:

1. Still not familiar with GIT

2. I still can't remember all funtions of the properties in CSS.

3. I don't know how to code JS so that when I enter data into the input box
and when I click add item, that data will be added to the list of tasks.

The return command returns a long piece of html code in js

let maxID = 0
        todo.forEach((item)=>{
            console.log(item.idTask);
            if(item.idTask > maxID){
                maxID = item.idTask; 
            }
        })
        console.log(maxID);
        idTest = maxID;
        return idTest;